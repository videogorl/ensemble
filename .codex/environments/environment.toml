# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "ensemble"

[setup]
script = ""

[[actions]]
name = "Run"
icon = "run"
command = '''
ROOT="/Users/felicity/.codex/worktrees/64f0/ensemble"
DERIVED="$ROOT/.derivedData/codex-sim"
DEVICE="iPhone 17 Pro"
BUNDLE_ID="com.videogorl.ensemble"

open -a Simulator
xcrun simctl boot "$DEVICE" 2>/dev/null || true
xcrun simctl bootstatus "$DEVICE" -b

xcodebuild \
  -workspace "$ROOT/Ensemble.xcworkspace" \
  -scheme Ensemble \
  -sdk iphonesimulator \
  -destination "platform=iOS Simulator,name=$DEVICE" \
  -derivedDataPath "$DERIVED" \
  build

xcrun simctl install "$DEVICE" "$DERIVED/Build/Products/Debug-iphonesimulator/Ensemble.app"
xcrun simctl launch "$DEVICE" "$BUNDLE_ID"
'''
